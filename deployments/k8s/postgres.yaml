apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: mini-k8ts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: mini_k8ts
            - name: POSTGRES_USER
              value: mini_k8ts
            - name: POSTGRES_PASSWORD
              value: mini_k8ts
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: mini-k8ts
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
